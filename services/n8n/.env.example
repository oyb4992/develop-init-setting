## n8n 로컬 개발용 환경 설정 파일 예시
##
# 이 파일을 `.env`로 복사한 후, 아래의 자리 표시자 값을
# 긴 임의의 문자열로 대체하십시오. 대부분의 시스템에서
# `openssl rand -hex 32` 명령어를 사용하여 임의의 문자열을 생성할 수 있습니다. 이 키는 사용자의
# 인증 정보를 암호화하므로 비밀로 유지해야 합니다. 실제 키가 포함된 `.env`
# 파일을 버전 관리 시스템에 커밋하지 마십시오.

N8N_ENCRYPTION_KEY=replace-with-a-long-random-string